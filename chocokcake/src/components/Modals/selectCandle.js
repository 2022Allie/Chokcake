import styled from "styled-components";

const SelectCandle = () => {
    return <Container>div</Container>;
};

export default SelectCandle;

const Container = styled.div`
    position: absolute;
    background-color: black;
    background-color: rgba(0, 0, 0, 0.65);
`;
